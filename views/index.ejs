<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title><%- title %></title>
    <link href="/style.css" rel="stylesheet" type="text/css">
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css">
    <meta name="description" content= <%- meta %> >
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        .bgtext {
            position: relative;
        }

        .bgtext:after {
            content: "<%- bgText %>";
            position: absolute;
            color: rgb(187, 182, 182);
            top: 0;
            left: 0;
            z-index: -1;
            <% if (hasResult) { %>
            <%- "filter: blur(2px);" %>
            <% } else {%>
            <%- "filter: blur(0.5px);" %>
            <% }%>
        }
    </style>
    <script data-ad-client="ca-pub-1015859928690519" async src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js"></script>
</head>
<body class="bgtext flow-text">
<%- include('search') %>
<% if (hasResult) { %>
    <%- include('result') %>
<% } %>
</body>
</html>
