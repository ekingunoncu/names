<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title><%- title %></title>
    <link href="/style.css" rel="stylesheet" type="text/css">
    <link href="https://fonts.googleapis.com/css2?family=M+PLUS+Rounded+1c:wght@300;400;500;700;800;900&display=swap" rel="stylesheet">
    <meta name="description" content= <%- meta %> >
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        .bgtext {
            position: relative;
        }

        .bgtext:before {
            content: "<%- bgText %>";
            position: absolute;
            color: rgb(187, 182, 182);
            top: 0;
            left: 0;
            z-index: -1;
            <% if (hasResult) { %>
            <%- "filter: blur(2px);" %>
            <% } else {%>
            <%- "filter: blur(0.5px);" %>
            <% }%>
        }
    </style>
    <script data-ad-client="ca-pub-1015859928690519" async src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js"></script>
</head>
<!-- body yapılır class="bgtext flow-text -->
<body class="<%- body_classes %>">
<%- include('top_bar') %>
<% if (!hasResult) { %>
<div class="logo"><img src="/logo.png">
<h1>Looking for a baby name that's uncommon and unique?</h1>
</div>
<div class="family"><img src="/family.png"></div>
<%- include('search') %>
<% } else { %>
<%- include('result') %>
<% } %>
<%- include('footer') %>
</body>
</html>
